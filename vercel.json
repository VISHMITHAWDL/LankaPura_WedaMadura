{
  "version": 2,
  "framework": null,
  "installCommand": "cd client && rm -rf node_modules package-lock.json && npm install --legacy-peer-deps",
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/dist",
  "build": {
    "env": {
      "NODE_VERSION": "20"
    }
  }
}